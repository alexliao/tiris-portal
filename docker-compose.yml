services:
  portal:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:80"
    restart: always
    image: ghcr.io/alexliao/tiris-portal:latest
    container_name: tiris-portal
